apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-cm
    app.kubernetes.io/part-of: argocd
data:
  # admin.enabled: "false"
  accounts.USERNAME(CHANGE_ME): apiKey, login # ADD USER NAME
  #   apiKey - allows generating API keys
  #   login - allows to login using UI

  # accounts.carlos.enabled: "false"

