apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: stage-public-api
  namespace: argocd
spec:
  destination:
    name: ''
    namespace: default
    server: 'https://kubernetes.default.svc'
  source:
    path: public-api/overlays/stage/
    repoURL: 'git@github.com:cachac/deploy-demo.git'
    targetRevision: HEAD
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
